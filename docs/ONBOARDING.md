# Onboarding

- Clone, run 'scripts/setup_julia.sh', then follow README Run section.
- Use 'make pressure-test' for a quick E2E sim + CSVs.
- PR rules: small diffs, add tests/validation where feasible, update docs.
